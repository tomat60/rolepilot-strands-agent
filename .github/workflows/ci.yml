name: CI

on:
  pull_request:
  push:
    branches: [main]

permissions:
  contents: read

jobs:
  test:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.10", "3.12"]
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
          cache: pip
      - name: Install
        run: python -m pip install -e '.[dev]'
      - name: Test
        run: pytest
      - name: Deterministic smoke
        run: rolepilot-agent --deterministic-smoke
      - name: Release verifier
        if: matrix.python-version == '3.12'
        run: python scripts/verify_release.py
      - name: Retain judge report for visual QA
        if: matrix.python-version == '3.12'
        uses: actions/upload-artifact@v4
        with:
          name: judge-report-${{ github.sha }}
          path: release-evidence/rolepilot-report.html
          if-no-files-found: error
          retention-days: 1
